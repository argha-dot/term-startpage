import { setCommand, clearCommands } from "./slices/commands";
import { setDirectory } from "./slices/directory";
import store from "./store";

export {
  store,
  setDirectory,
  setCommand,
  clearCommands
}
